# HYD 코드 리뷰 보고서

- 검토일: 2026-07-14
- 검토 범위: 저장소 전체 (`src/`, `public/`, 빌드 설정, 필수 제품 문서와 테스트), Git 이력, 두 참고 제품의 공식 자료·라이선스, 관련 법령·표준 고지
- 검토 방식: 코드/보안·명세 준수 리뷰와 아키텍처 리뷰를 독립 수행하고 핵심 경로를 직접 재현한 뒤, 공식 1차 자료로 저작권/IP 위험을 교차검증
- 최종 권고: **REQUEST CHANGES**
- 아키텍처 상태: **BLOCK**

## 요약

코어 엔진과 모델 테스트는 전반적으로 잘 구성되어 있고, `src/core/`의 React 비의존성, `Behavior.role` 기반 데이터 주도 솔버, 스키마 버전과 v1→v2 마이그레이션, 대표 시뮬레이션 골든 시나리오는 좋은 기반이다. 현재 `npm test -- --run`은 10개 파일의 50개 테스트가 모두 통과하고, `npm run build`도 성공한다.

그러나 외부 JSON 문서를 충분히 검증하지 않고 `CircuitDocument`로 단언하여 정상적인 파일 열기만으로 앱이 중단될 수 있다. PLC 출력에 릴레이·타이머·카운터를 선택할 수 있지만 해당 장치 상태가 갱신되지 않는 통합 결함도 재현됐다. 사용자가 제보한 릴리프 밸브는 더 직접적인 완료 차단 결함이다. 설정 압력과 P/T 포트를 제공하지만 실제 정의는 빈 연결의 `conduit`이고, 20bar와 80bar 설정의 런타임 결과가 완전히 동일해 현재는 작도만 가능한 요소다. 또한 PRD에 필수로 적힌 PLC 명령·디바이스와 일부 유압 부품/예제가 빠져 있어 “Phase 0~10 완료” 주장과 실제 범위가 일치하지 않는다.

후속 저작권/IP 감사에서는 이 프로젝트가 최초 문서에서 **Famic Technologies의 Automation Studio**와 **큐빅테크의 V-AMT**를 참고 모델로 명시했다가 5분 뒤 “저작권 우려”로 제품명과 링크만 삭제한 Git 이력을 확인했다. 현재 검토 범위의 코드·SVG·실행 화면·프로덕션 번들에서는 두 제품의 이름, 로고, 외부 그래픽, 독점 파일 포맷 또는 직접 복제 정황을 찾지 못했다. 그러나 저장소가 13,181 insertions의 완성본 최초 커밋으로 시작하고 자산별 제작 이력·참고 표준 판본·라이선스 기록이 없어, 저장소만으로 독립 창작 경위를 충분히 검증하기 어렵다. 이는 복제를 추정한다는 뜻이 아니라 출시 심사에 필요한 내부 증빙이 부족하다는 뜻이다. 또한 README가 공개 URL을 안내하고 GitHub Actions가 `main` push마다 Pages 배포하도록 구성되어 있으므로, 실제 공개 상태라면 이 게이트는 향후 계획이 아니라 **공개본에 대한 즉시 정리 과제**다.

## 검증 결과

| 항목 | 결과 |
|---|---|
| 프로덕션 빌드 | 성공: TypeScript 빌드 및 Vite 번들 생성 |
| 자동 테스트 | 성공: 10 files, 50 tests passed |
| 코어 경계 | `src/core/`에서 React/Zustand import 없음 |
| 잘못된 JSON 재현 | `parseDocument()`가 미등록 부품 타입을 `ok: true`로 수락한 뒤 엔진 생성 시 예외 발생 |
| PLC 릴레이 매핑 재현 | PLC 출력에 매핑한 릴레이 코일은 `energized: true`지만 같은 이름의 접점은 닫히지 않음 |
| 전체 레지스트리 스모크 | 47종(공압 20, 유압 15, 전기 12) 단독 초기화·1틱 모두 성공 |
| 내장 예제 스모크 | 18개 예제 build·20틱 실행 모두 성공 |
| 브라우저 수동 QA | 실제 Chrome에서 유압 기초 예제 로드·실행(압력계 40bar), 릴리프 배치와 설정 압력 50→20bar 편집 확인 |
| 경쟁 제품 런타임 감사 | 실제 Chrome과 `dist/`의 문자열·자산 검사에서 Automation Studio·V-AMT·Famic·Cubictek 명칭/로고 미발견; 번들에는 `XG5000` 문자열 존재 |
| 자산 감사 | 추적된 독립 이미지 파일은 `public/icon.svg`뿐이고 회로 기호·장비 그림은 React SVG primitive로 구성; 자산별 제작 provenance는 미기록 |
| Git 이력 감사 | 최초 커밋 `c794a8e`에서 두 제품을 참고 모델로 명시, 5분 뒤 `53b3bfc`에서 제품명/링크만 삭제; 코드·에셋 교체는 없음 |
| 공개 배포 상태 | `README.md:5`가 [GitHub Pages URL](https://nampard.github.io/hyd/)을 바로 사용하기로 안내하고 `.github/workflows/deploy.yml:4-46`이 `main` push 시 자동 배포. 본 리뷰에서는 공개 URL의 현재 도달 가능성까지 독립 확인하지 못함 |
| 의존성 취약점 조회 | 외부 npm 레지스트리로 프로젝트 메타데이터를 전송하는 정책 제한 때문에 수행하지 못함 |

## 요소별 기능 감사

판정 기준은 다음과 같다. **WORKING**은 기존 자동 테스트 또는 별도 런타임 프로브에서 정상 경로를 확인한 요소, **PARTIAL**은 핵심 경로는 동작하지만 재현 가능한 의미·연동·표시 결함이 있는 요소, **UNTESTED**는 실행 경로는 있으나 독립 행동 증거가 없는 요소, **BROKEN**은 사용자가 기대하는 핵심 기능이 실제로 없는 요소다. 단독 초기화 성공은 crash-smoke일 뿐 동작 정확성 증거로 계산하지 않았다.

전체 등록 부품은 47종(공압 20, 유압 15, 전기 12)이며 최종 통합 판정은 **WORKING 26 / PARTIAL 17 / UNTESTED 3 / BROKEN 1**이다. 모두 엔진 초기화와 1틱 실행에는 성공했고 18개 내장 예제도 20틱씩 예외 없이 실행됐다. 그러나 15종은 어떤 내장 예제에도 등장하지 않으며, 기존 50개 테스트가 통과해도 아래 결함은 검출되지 않는다.

### 사용자 제보: 릴리프 밸브

- `hyd.relief`는 5~300bar `pressure` 속성과 P/T 포트를 제공하지만 실제 실행 명세가 `{ role: "conduit", connections: [] }`이다(`src/core/library/hydraulic/index.ts:68-83`). 솔버는 빈 연결에서 아무 간선도 만들지 않는다(`src/core/sim/fluid-solver.ts:127-130`).
- 정상 병렬 회로(40bar 파워유닛→T분기→게이지/릴리프 P, 릴리프 T→탱크)에서 설정 20bar와 80bar 모두 게이지 40bar, 릴리프 P `pressurized/40`, T `exhausted/0`으로 완전히 동일했다.
- 실제 Chrome에서도 릴리프를 배치하고 설정 압력을 50→20bar로 편집할 수 있었다. 즉 설정 UI는 동작하지만 실행에는 소비되지 않는다. `HydRelief` 기호도 runtime을 무시해 개방 상태를 표현할 수 없다(`src/ui/symbols/index.tsx:702-714`).
- 유압 기초 예제와 전체 예제 목록에 릴리프가 없고 전용 테스트도 없다. ROADMAP의 “릴리프 압력 유지” 완료 설명과 실제 구현이 모순된다.
- 판정: **BROKEN / HIGH**. 현재 구조에서는 P↔T 정적 연결만 추가해도 무조건 탱크로 덤프되고 압력 상한은 적용되지 않는다. `Behavior`에 `{ role: "pressure-relief", pressurePort, tankPort, setpointProperty }` 같은 범용 명세를 추가하고, 솔버가 탱크/배기 도달 가능성과 후보 압력을 평가한 뒤 P 노드에 설정압 상한을 적용해야 한다. 등록 시 포트와 수치 속성 참조를 검증하고 구체 component type 분기는 금지한다. 작동 상태 표시와 설정 이하/초과 경계 테스트도 필요하다.

### 공압 20종

| 요소(type) | 판정 | 검토 결과 |
|---|---|---|
| 공압원 (`pneu.source`) | WORKING | 설정 압력 공급과 6bar 전파를 직접 검증 |
| 서비스 유닛 (`pneu.service-unit`) | UNTESTED | 손실·조절 없는 양방향 conduit로 구현, 예제/행동 테스트 없음 |
| 소음기 (`pneu.silencer`) | UNTESTED | 배기 terminal 경로는 있으나 성능 fixture 외 행동 검증 없음 |
| T 분기 (`pneu.tee`) | WORKING | OR/AND/파일럿/자동왕복 예제에서 3포트 연결 확인 |
| 3/2 수동 밸브 | WORKING | P→A/A→R 전환과 스프링 복귀 검증 |
| 3/2 롤러 밸브 | WORKING | 실린더 끝단 감지 자동왕복 검증. 중복 실린더 label은 첫 부품만 선택하는 별도 위험 존재 |
| 5/2 수동 밸브 | WORKING | 복동 실린더 방향 전환과 수동 토글 검증 |
| 5/2 양측 파일럿 | WORKING | X/Y 임펄스와 위치 기억 검증 |
| 5/2 편측 파일럿 | WORKING | 파일럿 전환과 신호 제거 후 스프링 복귀 검증 |
| 3/2 편측 솔레노이드 | PARTIAL | 코어 전환은 프로브 통과. 동일 label OR 결함과 장비 뷰 5/2 스프라이트 오바인딩 존재 |
| 5/2 편측 솔레노이드 | PARTIAL | 정상 단일 label 전환은 통과, 동일 label 복수 코일에서 순서 의존 |
| 5/2 양측 솔레노이드 | PARTIAL | 전기공압/PLC 예제는 통과, 동일 label OR와 동시 통전 의미가 불완전 |
| 5/3 양측 솔레노이드 | PARTIAL | 좌·중앙·우 전환은 프로브 통과, 양측 동시 ON이 현재 위치를 유지하며 경고 없음 |
| 복동 실린더 | WORKING | HEAD/ROD 압력·배기 조합과 `strokeTime` 적분을 광범위하게 검증 |
| 단동 실린더 | WORKING | 가압 전진과 배기 시 스프링 복귀 검증 |
| 속도제어밸브 | WORKING | A→B 자유, B→A `openness` 교축과 실제 속도 차이 검증 |
| 셔틀밸브 OR | PARTIAL | 출력 OR은 동작하지만 X1만 가압해도 X2와 비활성 입력 회로가 역가압됨 |
| 2압밸브 AND | PARTIAL | 논리 AND는 동작하지만 X1=6bar/X2=2bar에서 출력이 최소값 2bar가 아닌 6bar |
| 급속배기밸브 | UNTESTED | 동적 P→A/A→R 경로는 있으나 예제·직접 회귀 테스트 없음 |
| 공압 압력 스위치 | WORKING | 6bar/4bar 임계 NO/NC 프로브 통과, 기존 자동 테스트는 유압형 중심 |

공압의 우선 결함은 두 가지다. 셔틀밸브는 선택된 입력만 출력으로 공급해야 하지만 현재 간선을 양방향으로 추가해 출력에서 비활성 입력으로 역급기한다(`src/core/sim/fluid-solver.ts:205-215`). 2압밸브는 두 입력이 켜진 뒤 `inA`만 출력과 연결하여 압력 레벨을 한쪽 값에서 가져온다(`src/core/sim/fluid-solver.ts:216-224`). 상태 기반 논리와 별개로 이미 bar 레벨을 표시하는 제품이므로 두 입력 중 낮은 압력 상한을 적용하는 편이 일관적이다.

### 유압 15종

| 요소(type) | 판정 | 검토 결과 |
|---|---|---|
| 파워유닛 (`hyd.power-unit`) | WORKING | 설정 압력 P 공급과 T 귀환 terminal 검증 |
| 탱크 (`hyd.tank`) | WORKING | 귀환/배기 terminal 검증 |
| T 분기 (`hyd.tee`) | WORKING | 기초·감압 예제의 3포트 공통 연결 검증 |
| 압력계 (`hyd.gauge`) | PARTIAL | 실제 bar 숫자는 정상. 시스템 공급 설정은 최대 300bar인데 바늘은 100bar에서 포화(`src/ui/symbols/index.tsx:682-697`) |
| 릴리프 (`hyd.relief`) | **BROKEN** | 설정값을 전혀 읽지 않는 빈 conduit, 작도만 가능 |
| 4/2 수동 밸브 | WORKING | P-A/B-T ↔ P-B/A-T 전환과 실린더 왕복 검증 |
| 4/3 클로즈드 센터 | WORKING | 무신호 전체 차단, 좌우 솔레노이드 조그 검증 |
| 4/3 탠덤 센터 | PARTIAL | A/B 홀드는 되지만 중립 P↔T에서 탱크와 T가 40bar `pressurized`로 표시됨 |
| 체크밸브 (`hyd.check`) | WORKING | 별도 프로브에서 A→B 40bar 전달, B→A 차단 확인. 자동 예제·테스트는 없음 |
| 파일럿 체크 (`hyd.pilot-check`) | WORKING | 역방향은 파일럿 없음 시 차단, X=40bar에서 개방 확인. 자동 예제·테스트는 없음 |
| 유량조절밸브 | WORKING | 미터아웃 교축과 실린더 속도 차이 검증 |
| 감압밸브 (`hyd.reducing`) | PARTIAL | 정방향 40→20bar는 통과하지만 역방향에도 같은 cap을 적용해 방향 의미가 잘못됨(`src/core/sim/fluid-solver.ts:114-122`) |
| 유압 압력 스위치 | PARTIAL | 20/10bar와 15bar 임계의 NO/NC 및 램프 연동은 첫 틱 후 통과. 초기 snapshot의 접점 상태가 잘못됨 |
| 복동 유압 실린더 | WORKING | 4/2·4/3·유량조절 회로에서 위치 적분 검증 |
| 유압 모터 | WORKING | A/B 압력 방향에 따른 정·역회전 검증 |

탠덤 센터의 P↔T 중립 경로 자체는 정의되어 있지만, 공급과 배기가 연결되면 솔버가 가압을 우선하여 탱크 귀환을 `pressurized`로 표시한다(`src/core/sim/fluid-solver.ts:244-270`). 또한 체크·파일럿 체크·탠덤 센터는 고유 동작 테스트가 없고, PRD의 4/3 오픈센터와 ROADMAP의 카운터밸런스는 등록조차 되어 있지 않다.

### 전기 12종 및 PLC 연동

| 요소(type) | 판정 | 검토 결과 |
|---|---|---|
| +24V (`elec.supply-24v`) | WORKING | positive 도달성 시작점 및 전원 누락 차단 검증 |
| 0V (`elec.supply-0v`) | WORKING | negative 도달성 시작점 및 전원 누락 차단 검증 |
| 푸시버튼 (`elec.pushbutton`) | WORKING | NO/NC·momentary·maintained 코어 동작 검증, 실제 포인터 UI 회귀 테스트는 없음 |
| 리밋 스위치 | WORKING | 전진/후진단 NO/NC 전환 검증 |
| 릴레이 접점 | PARTIAL | 직접 회로 label 연동은 정상, PLC에 매핑한 릴레이/타이머/카운터 출력은 접점을 구동하지 못함 |
| 릴레이 코일 | PARTIAL | 직접 회로와 같은 label OR는 정상, PLC 출력 경로는 runtime 표시만 켜짐 |
| 타이머 | PARTIAL | TON/TOFF 상태기는 동작하지만 출력 접점 반영이 한 틱 늦고 TOFF 예제·테스트/기호 구분 없음 |
| 카운터 | PARTIAL | 상승 에지 계수는 프로브 통과, preset 도달 접점이 다음 틱에 반영되고 count를 snapshot/UI에서 볼 수 없음 |
| 카운터 리셋 | PARTIAL | 리셋은 직접 회로에서 다음 틱 반영, PLC 매핑 경로는 동작하지 않음 |
| 솔레노이드 | PARTIAL | 단일 label 공압·유압 연동은 정상, 동일 label 복수 코일은 문서 순서에 따라 신호 소실 |
| 램프 | WORKING | 직접 전원·릴레이·압력 스위치·PLC 출력 점등 검증 |
| 부저 | PARTIAL | generic load 통전과 SVG 활성 표시는 동작하지만 실제 음향 출력 구현·예제·테스트 없음 |

전기/PLC의 교차 결함은 다음과 같다.

- 모든 `elec-load`의 `label`을 장치 코일 입력으로 합산해, 예를 들어 label `K1`인 솔레노이드만 통전돼도 K1 릴레이 접점이 닫힌다(`src/core/sim/engine.ts:221-245`). 장치 종류별 입력 채널을 분리해야 한다.
- 동일 label 솔레노이드 집합은 OR가 아니라 각 부품의 add/delete 순서로 결정된다(`src/core/sim/engine.ts:233-236`). ON 코일 뒤의 OFF 코일이 활성 label을 삭제한다.
- 타이머/카운터는 전기 고정점 해석 뒤 상태를 갱신하고 재솔브하지 않아 경계 시점보다 1틱(현재 20ms) 늦게 접점이 바뀐다(`src/core/sim/engine.ts:253-295`).
- PLC 출력은 부품 runtime만 덮어쓰고 장치 상태와 전기 회로를 재평가하지 않는다(`src/core/sim/engine.ts:298-325`). PLC→램프/솔레노이드는 동작하지만 PLC→릴레이/타이머/카운터/리셋은 실패한다.
- XG5000 스타일 래더의 NO/NC, OUT, SET/RST, TON, CTU와 기본 직·병렬 스캔은 동작한다. 그러나 PRD 필수 범위의 TOFF, CTD, D 디바이스는 구현되어 있지 않다.
- P 주소의 입력/출력 범위를 검증하지 않고 스캐너가 모든 P 비트를 출력 이미지에 포함한다. 같은 P0를 입력과 출력에 매핑하면 OUT 렁 없이 입력 상태만으로 램프가 켜졌다(`src/core/plc/scanner.ts:50-54`).
- 출력 코일 셀의 통전 강조 조건이 오른쪽 노드까지 true일 것을 요구하지만 스캐너는 출력 셀 오른쪽 노드를 켜지 않아, 실제 M0/P20과 램프가 ON이어도 코일 셀이 흰색으로 남는다. vlink도 통전 색상을 표현하지 않고 마지막 행에 무효 링크를 만들 수 있다.
- T/C 디바이스의 경과시간·현재 계수·완료 상태가 snapshot/UI에 없어 코일 입력과 완료 출력을 구분하기 어렵다.
- label, 실린더 label, PLC I/O device를 오타 내도 `validation=[]`로 조용히 실패한다. 교차 참조 적합성 검사가 필요하다.

### UI/장비 뷰 교차 확인

- 등록 47종 모두 팔레트와 회로 기호 레지스트리에는 나타난다. 다만 기호 누락 시 `?` 폴백만 하며 레지스트리 정합성 테스트는 없다.
- `pneu.valve.3-2-solenoid` 장비 뷰가 5포트용 `makeValve52Sprite("sol-single")`에 잘못 연결되어 3/2 밸브를 5/2처럼 표시한다(`src/ui/equipment/sprites.tsx:130-161,226-238`).
- 실제 Chrome에서 유압 기초 예제 로드, 실행 버튼 상태 전환, 압력계 40bar 표시, 새 회로, 릴리프 배치, 설정 압력 편집을 확인했다. 예제 선택 UI에도 릴리프·체크·파일럿 체크·탠덤 센터 전용 시나리오는 없다.

## 참고 제품 및 저작권/IP 감사

> 이 절은 저장소·공개 자료를 바탕으로 한 저작권·계약·표장 위험의 1차 출시 심사이며 법률 자문이나 침해 여부의 확정 판단이 아니다. 특허·디자인권·영업비밀·부정경쟁, 등록상표 권리조사, 해외 배포국 법률은 별도 검토 대상이다. Famic EUSLMA를 수락한 개발자가 있다면 약관의 준거법·관할 조항 때문에 한국 법률 검토만으로 충분하지 않을 수 있다.

### 두 참고 제품과 기능적 중첩

Git 이력상 두 참고 제품은 다음과 같이 특정된다.

| 참고 제품 | 공식 제품 범위 | HYD와 겹치는 기능적 아이디어 | 현재 확인된 표현 복제 증거 |
|---|---|---|---|
| [Automation Studio Educational Edition](https://www.famictech.com/en/Products/Automation-Studio/Educational-Edition) — Famic Technologies | 공압·유압·전기·PLC 회로 작성, 시뮬레이션·애니메이션·고장 진단, 부품 라이브러리, 가상 장비·PLC 연동 | 다중 도메인 회로 편집기, 심볼 팔레트, 상태 애니메이션, PLC 교육 | 검토 범위에서 미발견. 문자열·자산·런타임 감사 결과이며 비문언적 UI·작업 흐름 유사성까지 배제하지는 않음 |
| [V-AMT](https://www.cubictek.co.kr/sub/sub02_01.php?cat_no=2&idx=3&mode=view&offset=) — 큐빅테크 | 공압·유압·전기·PLC 직업훈련, 2D 회로 작도, 3D 가상 실습, LS/미쓰비시 PLC 연동, 자격증 예제. 공식 페이지에는 릴리프·감압·카운터밸런스 등도 명시 | 2D 회로 작도와 장비 뷰 병행, 자격증 연습, LS 계열 PLC 표기, 부품 팔레트 | 검토 범위에서 미발견. 3D 구현, 제품 문구·로고·도움말·프로젝트 파일·외부 그래픽의 문자열·자산 감사 결과 |

공압·유압 회로를 작도하고 상태 기반으로 시뮬레이션한다는 목적과 기능 자체가 곧 저작권 침해를 뜻하지는 않는다. WIPO도 저작권은 아이디어·절차·작동 방법이 아니라 구체적인 표현을 보호한다고 설명한다([WIPO 저작권 보호 안내](https://www.wipo.int/en/web/copyright/protection), [소프트웨어 저작권 안내](https://www.wipo.int/en/web/copyright/activities/software)). 반대로 창작성이 있는 소스코드, SVG·아이콘, 화면 구성, 설명문, 예제 파일과 카탈로그의 선택·배열은 보호 대상이 될 수 있다. 다만 개별 사실·수치, 단순·기능적 요소, 표준 규칙의 보호 여부는 별도 판단이며 데이터베이스제작자 권리는 저작권과 다른 요건을 가진다.

### 저장소와 이력이 말하는 것

- 최초 커밋 `c794a8edf8932bebd661cce87924734b33343433`의 `README.md`와 `CLAUDE.md`는 Automation Studio를 “회로도 중심 공학 시뮬레이션”, V-AMT를 “가상 실습장비 오퍼레이팅” 참고 모델로 직접 명시했다. 같은 커밋의 PRD에는 “V-AMT 방식”이라는 표현도 있었다.
- 5분 뒤 커밋 `53b3bfcf0c881582cc2f1c965f53f78acdec1c89`는 “저작권 우려로 Automation Studio(Famic Tech), V-AMT(큐빅테크) 제품명과 링크를 문서에서 삭제”했다고 기록한다. 변경은 문서 표현 세 곳뿐이며 코드·SVG·예제 교체는 없었다.
- 현재 `README.md:7`은 실명 대신 상용 시뮬레이션의 장점을 참고한 독자 도구라고 설명한다. 제품명을 삭제한 사실은 침해 증거도 아니고 독립 창작 증거도 아니다. 이미 복제된 표현이 있다면 이름 삭제로 치유되지 않고, 단순한 기능 벤치마킹이었다면 이름을 썼다는 이유만으로 침해가 되지도 않는다.
- 소스와 빌드 산출물의 문자열·자산 검사에서 Automation Studio, V-AMT, Famic, Cubictek/큐빅테크를 찾지 못했다. 실제 Chrome의 메인·PLC 화면에서도 두 제품명이나 로고를 관찰하지 못했다. 추적된 독립 이미지 파일은 `public/icon.svg` 하나이고 기호와 장비 그림은 JSX의 선·원·사각형 등 SVG primitive로 작성되어 있다. 이는 검토 범위 내 미발견 결과이며 비문언적 유사성이나 저장소 밖 자료까지 배제하지 않는다.
- 다만 최초 커밋이 66개 파일·13,181 insertions의 완성본으로 시작해 그 이전의 스케치, 개별 자산 제작, UI 설계 의사결정, 표준 대조 기록을 Git에서 검증할 수 없다. 최초 커밋 이전 자료가 저장소에 없으므로 저장소만으로 독립 창작 경위를 충분히 검증하기 어렵다는 의미이며 복제를 추정하는 것은 아니다.
- 공개 배포 경로는 이미 저장소 운영 범위에 들어와 있다. `README.md:5`는 GitHub Pages URL을 안내하고 `.github/workflows/deploy.yml:4-46`은 `main` push마다 테스트·빌드 후 Pages에 배포하도록 구성돼 있다. 본 리뷰는 현재 URL 도달성과 최근 배포 성공을 확인하지 못했으므로, 아래 권고는 실제 Pages가 활성화되어 있다면 공개본의 증빙·고지 정리에 즉시 적용하고 그렇지 않다면 재공개 전 게이트로 적용한다.

### 권리·계약 경계

- Automation Studio의 [EUSLMA Rev.18](https://www.famictech.com/Portals/0/PDF/F7510105-EN%20Automation%20Studio%20EUSLMA%20V18.pdf)은 설치 과정에서 약관을 수락한 라이선시에게 적용되고, 제18조는 소프트웨어·문서의 무단 복제·변형·번역·역공학·디컴파일·파생작품 제작 등을 제한한다. 반면 제11조는 라이선시가 소프트웨어로 만든 프로젝트·콘텐츠를 라이선시 소유로 규정하므로 적법한 제품 사용이나 매뉴얼 열람 자체가 곧 계약 위반은 아니다. 약관을 수락한 HYD 개발자가 소프트웨어·문서의 표현을 복제·변형했거나 제한 자료를 허용 범위 밖에서 이용했다면 계약 검토가 필요하다. 제30조의 퀘벡법·몬트리올 관할도 함께 고려해야 한다.
- 본 검토에서는 공식 공개 페이지에서 V-AMT 제품 EULA를 확인하지 못했다. 큐빅테크 [홈페이지 이용약관](https://www.cubictek.co.kr/member/terms.php) 제13조는 “회원”이 서비스에서 얻은 정보를 이용하는 경우를 규율하므로 이를 V-AMT 제품 라이선스나 모든 방문자에게 당연히 적용되는 조건으로 확대해서는 안 된다. 실제 구매계약, 설치 시 동의한 약관, 매뉴얼 권리고지는 미확정 항목이다.
- 교육 목적은 자동 면책 사유가 아니다. 저작권법 제35조의5는 프로그램 관련 예외 외에 적용되는 공정 이용 조항으로 목적·성격, 저작물 종류·용도, 이용 비중·중요성, 시장 영향을 함께 고려한다([제35조의5](https://law.go.kr/lsLinkCommonInfo.do?chrClsCd=010202&lsJoLnkSeq=1025203559)). 프로그램에는 수업 목적 복제 등 제101조의3과 제한적 호환 목적 코드역분석을 허용하는 제101조의4도 있으나 각각 엄격한 요건이 있고 독립 시뮬레이터의 일반 배포가 당연히 해당하는 것은 아니다([현행 저작권법](https://www.law.go.kr/LSW/lsInfoP.do?ancYnChk=0&chrClsCd=010202&efYd=20260511&lsiSeq=283335&urlMode=lsInfoP)). 원제품 표현물을 포함했다면 동일 교육 용도에서의 시장 영향이 불리한 요소가 될 수 있다는 것이 본 리뷰의 위험 추론이다.
- 데이터베이스제작자 권리는 데이터베이스 제작·갱신 등에 인적·물적으로 상당한 투자를 한 자에게 인정된다. 해당 요건을 갖춘 원제품 데이터베이스의 전부·상당 부분을 추출하거나, 소량이라도 반복적·체계적으로 이용해 일반적 이용과 충돌하거나 제작자 이익을 부당하게 해친 경우 제93조 검토가 필요하다. 제93조제4항에 따라 이 권리가 개별 부품명·설정값 자체까지 보호하는 것은 아니다([국가법령정보센터](https://www.law.go.kr/lsLinkCommonInfo.do?chrClsCd=010202&lsJoLnkSeq=1029423451)).

### 표준·교육자료 기반 자산

`README.md:16`, `docs/PRD.md:67,73`과 `src/ui/symbols/index.tsx:417-445` 등은 ISO 1219 기반 기호를 명시한다. [ISO 1219-1:2012](https://www.iso.org/standard/60184.html)은 유공압 그래픽 기호의 기본 요소와 작성 규칙, [ISO 1219-2:2012](https://www.iso.org/standard/51200.html)은 회로도 작성 규칙을 다룬다. 저장소에는 실제 참조 판본, ISO 1219-1 Amendment 1:2016 반영 여부, 구매/열람 주체, 기호별 독립 작도 일자와 원본 스케치가 없다.

ISO의 저작권 고지는 표준 출판물과 그 콘텐츠의 복제에 서면 허락이 필요하다고 설명한다([ISO 저작권 고지](https://www.iso.org/copyright.html)). 그러나 이 고지만으로 독립 작성된 모든 단순·기능적 기호가 곧 침해라고 결론 낼 수는 없다. HYD 기호가 ISO 도면에서 복제·트레이싱됐다면 이용 허락 검토가 필요하고, 독립 작도라면 보호되는 표현의 이용 여부를 별도로 판단해야 한다. 현재 JSX가 외부 SVG를 임베드하지 않고 기본 도형으로 직접 구성된 점은 유리한 정황이지만 정확한 판본·참조 자료·작성 경위 기록이 필요하다. 현재 BLOCK은 침해 판정이 아니라 출처 미확인에 대한 내부 출시 게이트다.

ISO 외의 교육자료 provenance도 미확정이다. `docs/PRD.md:73`은 전기 기호가 “KS/시퀀스 제어 교재 표기”를 따른다고 하지만 정확한 KS 번호·판본·교재·저자·출판사·이용 근거가 없다. `docs/PRD.md:27`과 `src/core/examples/index.ts:398-401`은 자격증 기출 유형·공유압기능사 유형 예제를 제공하지만 원문제를 복제한 것인지 독립 구성한 것인지 출처 기록이 없다. 실제 복제 증거가 있다는 뜻은 아니며, 표준·교재·시험자료별 작성 경위를 provenance 원장에 포함해야 한다.

### 출시 위험 판정

| 항목 | 위험도 | 판정 |
|---|---:|---|
| 다중 도메인 회로 편집·상태 시뮬레이션이라는 기능적 아이디어 | 낮음 | 독립 구현이면 원칙적으로 허용 가능성이 높음 |
| 현재 React/TypeScript 솔버와 JSX SVG에서 원제품 코드·파일·로고의 직접 복제 | 낮음/CLEAR | 검토 범위에서 미발견. 비문언적 유사성·저장소 밖 자료까지 배제하지 않음 |
| 원제품 매뉴얼 문구·스크린샷·아이콘·SVG·예제 프로젝트·카탈로그 재사용 | 미확정/출시 게이트 | 현재 미발견. 보호되는 원본 표현을 허락이나 적용 가능한 법정 예외 없이 재사용했다면 제거·재작성 또는 권리 확보 필요; 단순 사실·비보호 아이디어·적법한 이용은 별도 판단 |
| UI 색상·도구 배열·아이콘·작업 흐름의 전체적 유사성 | 중간/UNKNOWN | 공식 화면과의 통제된 side-by-side 및 초기 디자인 자료가 없어 결론 불가 |
| 부품 데이터·파라미터의 대량 추출 | 높음/UNKNOWN | 데이터 출처표가 없어 독립 조사인지 원제품 카탈로그 추출인지 검증 불가 |
| ISO 1219 기호의 판본·참조·이용 범위 | UNKNOWN/내부 게이트 BLOCK | 기본 도형 직접 작성 정황은 있으나 출처·권리 확인 기록 없음. ISO 출판물 복제와 독립 작도는 구분해야 함 |
| KS·교재 기호와 자격시험 유형 예제 | UNKNOWN/내부 게이트 BLOCK | 정확한 표준·교재·시험자료 출처와 독립 구성 기록 없음 |
| Automation Studio/V-AMT 명칭·로고 사용 | 낮음/CLEAR | 현재 제품 UI·번들에서 미발견. 사실관계 설명용 본 리뷰에서만 사용 |
| XG5000/LS 계열 표기와 제휴·호환성 오인 | 중간/WATCH | `.xgp` 호환은 제외했지만 README·PRD·코드·번들에 명칭이 남고 비제휴 고지가 없음 |
| 프로젝트 자체 라이선스·NOTICE | 중간/WATCH | `package.json`은 private이고 루트에 LICENSE/NOTICE가 없어 제3자의 재사용·배포 조건과 고지 범위가 불명확. 소유자의 배포 자체를 곧바로 막는 사유로 보지는 않음 |
| 런타임 제3자 패키지 고지 | UNKNOWN/내부 게이트 BLOCK | React·React DOM·Zustand는 MIT이나 배포물의 라이선스 고지 절차·SBOM이 없음. 개발도구 라이선스와 런타임 포함물을 구분한 확인 필요 |
| 코드·문서·자산 권리 귀속(chain of title) | UNKNOWN/내부 게이트 BLOCK | Git 작성자 정보만으로 고용·위탁·양도·외부 기여·생성 도구 사용에 따른 권리 귀속을 증명할 수 없음 |

종합 판정은 **검토 범위 내 직접 복제 정황 미발견 / 침해 여부 미확정 / 출처·표준 이용범위·권리 귀속 확인 전 공개 경로 활성화·재공개 및 향후 상용 배포 내부 게이트 BLOCK**이다. 이 BLOCK은 법적 침해 판정이 아니라 출시 증빙 부족에 대한 보수적 내부 통제다.

### 공개 경로 활성화·재공개 및 후속 배포 필수 조치

1. `ASSET_PROVENANCE.md` 또는 동등한 원장에 모든 SVG, 장비 스프라이트, 아이콘, 도움말, 예제 회로, 부품 파라미터별 `작성자 / 작성일 / 출처 / 참고 표준·판본 / 라이선스·허락 / 원본 파일`을 기록한다. KS·교재·자격시험 유형 예제도 별도 항목으로 포함한다.
2. 기여자별로 Automation Studio·V-AMT 설치본, 매뉴얼, 카탈로그, 스크린샷, 예제 프로젝트에 접근했는지와 실제로 추출·전사·트레이싱한 항목, 이용 근거를 기록한다. 코드역분석이 있었다면 목적·범위·약관 수락 여부를 별도로 남기고 법정 예외와 계약 제한의 관계는 관할별 법률 검토로 판단한다.
3. 현재 UI와 두 제품의 공개 화면을 독립 리뷰어가 side-by-side로 비교하되, 기능 목록과 일반적인 CAD/IDE 배치가 아니라 창작적인 아이콘·색상·배열·흐름의 실질적 유사성을 검토하고 결과를 보관한다.
4. V-AMT의 실제 구매계약/EULA와 매뉴얼 권리고지, ISO 1219 기호 사용 범위를 확보해 전문 법률 검토를 받는다.
5. 프로젝트 LICENSE와 자산 라이선스를 정하고, 런타임 패키지의 `이름 / 버전 / 라이선스 / 저작권 고지 / 배포물 내 고지 위치`를 담은 SBOM/NOTICE를 만든다. 개발도구의 라이선스는 최종 번들 포함 여부와 구분한다.
6. 현재 외부에 노출되지 않는 Automation Studio·V-AMT 명칭을 고지 목적으로 새로 추가하지 않는다. 계속 사용하는 XG5000·LS ELECTRIC 표기에는 필요한 위치에서 중립적 출처 식별과 비제휴·`.xgp` 비호환 고지를 두고 로고는 사용하지 않는다. 고지는 혼동 위험을 줄이는 보조수단이지 무단 사용이나 표현 복제를 치유하지 않는다.
7. 제품명 삭제 커밋은 숨기거나 재작성하지 않는다. 현 상태를 소급해 “클린룸”이라고 부르지 말고 “참고 자료의 접근 범위를 기록한 독립 개발” 원칙으로 설명한다. 향후 인력·자료 접근 분리와 동시대 기록을 실제 운영한 경우에만 클린룸이라는 표현을 사용한다.
8. 코드·문서·자산 작성자의 고용·위탁 관계, 권리 양도, 외부 기여, 생성 도구 사용과 검토 기록을 확인해 chain-of-title 문서로 남긴다. 단일 Git 작성자 표시는 권리 귀속 증명이 아니다.
9. Pages가 현재 활성화되어 있다면 공개본을 유지하는 동안 위 미확정 항목과 내부 게이트 상태를 프로젝트 책임자가 명시적으로 인수한다. 비활성 상태라면 재공개 전, 활성 상태라면 후속 `main` 배포 전에 provenance·고지 정리 여부를 재점검한다. 본 리뷰는 배포 상태 변경을 실행하지 않았으며 정책 결정은 프로젝트 책임자에게 남긴다.

## 발견 사항

### CRITICAL (0)

확인된 치명적 보안 취약점은 없다. 이 앱은 백엔드, 인증, SQL, 서버 렌더링이 없는 로컬 프론트엔드이므로 일반적인 서버 측 주입 공격면은 작다. 다만 의존성 취약점 데이터베이스 조회는 위 제한 때문에 검증 범위에서 제외됐다.

### HIGH (6)

#### H1. 외부 문서 검증이 불완전해 잘못된 JSON이 앱을 중단시킨다

- 근거: `src/core/model/schema.ts:18-50`은 `schemaVersion`, `components`/`wires` 배열, `meta.title`만 확인하고 `migrated as unknown as CircuitDocument`로 반환한다.
- 하류 경로: `src/ui/editor/Toolbar.tsx:23-27`이 반환 문서를 즉시 상태에 넣고, `src/ui/editor/ComponentView.tsx:59`와 `src/core/library/registry.ts:12-15`가 미등록 타입에서 예외를 던진다.
- 직접 재현: 미등록 `type: "unknown.type"`을 가진 문서가 `parseDocument()`에서 `ok: true`가 된 뒤 `SimulationEngine` 생성 시 `등록되지 않은 부품 타입` 예외가 발생했다.
- 영향: 손상되거나 수동 편집된 JSON, 미래/타 도구에서 생성한 파일 하나로 편집 화면 또는 실행이 중단될 수 있다. 파일 포맷이 핵심 저장·교환 경계라는 점에서 아키텍처 차단 사유다.
- 권고: 문서 전체를 경계에서 파싱한다. 부품 필드와 등록 타입, 회전/좌표/속성, ID 유일성, 배선 끝점·포트 존재·kind 일치, PLC/ioMap 참조, equipmentLayout을 검증하고 성공/실패가 구분되는 판별 유니온을 반환한다. 잘못된 중첩 객체·미등록 타입·끊어진 참조 회귀 테스트를 추가한다.

#### H2. PLC 출력에 매핑한 릴레이·타이머·카운터가 접점을 구동하지 못한다

- 근거: UI는 모든 `elec-load`를 출력 후보로 허용한다(`src/ui/plc/PlcPanel.tsx:166-172`).
- 구현: `src/core/sim/engine.ts:312-325`는 PLC 출력을 `runtime.energized`에 쓰지만, 장치 상태 갱신은 솔레노이드만 특별 처리한다. 릴레이/타이머/카운터의 `DeviceState.coil/output`은 갱신되지 않는다.
- 직접 재현: 무조건 ON인 `P20`을 `elec.relay-coil`에 매핑했을 때 코일 스냅숏은 `energized: true`였지만 같은 `K1` 릴레이 접점은 `contactClosed: false`였다.
- 영향: UI상 허용되는 PLC→릴레이/타이머/카운터 회로가 조용히 오동작한다.
- 권고: 출력 후보를 실제 지원 대상(현재 PRD상 솔레노이드·램프)으로 제한하거나, PLC 출력을 장치 코일 집계에 포함한 뒤 타이머/카운터 전이와 후속 전기 재솔브까지 일관되게 수행한다. 각 부하 종류별 통합 테스트를 추가한다.

#### H3. PRD 필수 PLC 범위가 구현되지 않았는데 완료로 표시되어 있다

- 요구: `docs/PRD.md:53-59`는 최소 `D` 디바이스와 `TOFF`, `CTD`를 필수 범위로 명시한다.
- 실제: `src/core/plc/model.ts:16-24`, `src/core/plc/scanner.ts:105-143`, `src/ui/plc/PlcPanel.tsx:26-36`에는 TON/CTU까지만 있다.
- 영향: 오프딜레이, 다운카운터, 최소 데이터 디바이스를 사용하는 XG5000 교육 회로를 작성하거나 실행할 수 없다.
- 권고: 모델·스캐너·에디터·모니터에 D/TOFF/CTD를 구현하고 경계 테스트를 추가한다. 의도적 범위 축소라면 PRD와 완료 표기를 먼저 실제 범위에 맞춘다.

#### H4. 유압 Phase 완료 주장과 실제 부품·예제 범위가 다르다

- 요구: `docs/PRD.md:46-51`은 4/3 오픈센터를 포함한다. `docs/ROADMAP.md:35-40`은 PRD 4.3 라이브러리와 미터인/아웃, 카운터밸런스 예제를 완료했다고 적는다.
- 실제: `src/core/library/hydraulic/index.ts:129-181,327-343`에는 4/3 클로즈드/탠덤 센터만 있고 오픈센터·카운터밸런스 부품이 없다. 예제에도 미터인·카운터밸런스 시나리오가 없다.
- 영향: 문서를 기준으로 수업을 준비하는 사용자가 완료됐다고 표시된 실습을 찾을 수 없다.
- 권고: 누락 부품과 예제·골든 테스트를 구현하거나, ROADMAP/README 완료 주장을 실제 지원 범위로 정정한다.

#### H5. 새 회로와 JSON 열기가 저장되지 않은 현재 작업을 확인 없이 폐기한다

- 근거: 브라우저 저장본 열기와 예제 열기는 확인 대화상자를 거친다(`src/ui/editor/Toolbar.tsx:62-101`). 반면 JSON 파일 열기는 즉시 `loadDocument()`를 호출하고(`src/ui/editor/Toolbar.tsx:23-27`), 새 회로 버튼도 즉시 `newDocument()`를 호출한다(`src/ui/editor/Toolbar.tsx:143-147`).
- 영향: 사용자가 작업 중 실수로 새 회로나 열기를 누르면 되돌릴 수 없는 로컬 작업 손실이 발생한다.
- 권고: 문서 dirty 상태를 추적하고 새 회로/파일 열기/예제/브라우저 저장본 열기 모두 동일한 미저장 변경 보호 정책을 사용한다.

#### H6. 릴리프 밸브의 설정 압력과 안전 기능이 실행되지 않는다

- 근거: `src/core/library/hydraulic/index.ts:68-83`은 `pressure` 속성과 P/T 포트를 노출하지만 behavior는 빈 `conduit`이다. `src/core/sim/fluid-solver.ts:127-130`은 선언된 연결만 간선으로 만들므로 이 부품은 실행에 아무 영향도 주지 않는다.
- 엔진 프로브 직접 재현: 40bar 파워유닛과 병렬 릴리프·탱크 회로에서 설정을 20bar/80bar로 바꿔도 게이지는 모두 40bar였고 릴리프 P/T 상태도 동일했다. 별도의 실제 브라우저 QA에서는 유압 기초 예제의 40bar 표시와 릴리프 속성 50→20bar 편집이 가능함을 확인했지만, 편집값이 실행 동작에 소비된다는 증거는 없었다.
- 관찰성: `src/ui/symbols/index.tsx:702-714`의 기호는 properties/runtime을 모두 무시한다. 사용자는 설정값을 편집할 수 있지만 개방 여부나 압력 제한 상태를 확인할 수 없다.
- 영향: 유압 안전 요소로 제공되고 ROADMAP에서 완료됐다고 표시된 핵심 부품이 작도 외에는 동작하지 않는다.
- 권고: `Behavior`가 pressure/tank 포트와 설정압 속성 참조를 소유하도록 범용 `pressure-relief` 역할을 정의한다. 솔버는 탱크 도달 가능성을 확인하고 후보 압력이 설정값을 넘을 때 P 노드에 generic pressure cap을 적용해야 한다. 등록 시 포트·수치 속성 참조를 검증하고 구체 type 분기는 추가하지 않는다. 설정 이하/초과/탱크 미연결 경계 및 브라우저 표시 테스트를 추가한다.

### MEDIUM (11)

#### M1. “첫 방문 후 오프라인” 동작이 현재 서비스 워커로 보장되지 않는다

- 근거: 서비스 워커는 페이지 `load` 후 등록된다(`src/main.tsx:9-16`). install 단계는 앱 셸을 캐시하지 않고(`public/sw.js:8-10`), 활성화된 뒤 가로챈 성공 응답만 캐시한다(`public/sw.js:22-34`).
- 영향: 첫 페이지의 HTML/JS/CSS 요청은 서비스 워커 활성화 전에 끝날 수 있어, 한 번 방문한 직후 오프라인 재로드가 실패할 수 있다.
- 권고: 빌드 해시 기반 precache 또는 install 단계 앱 셸 캐시를 사용하고, 깨끗한 프로필에서 첫 방문→오프라인→재로드 E2E를 추가한다.

#### M2. 같은 라벨을 공유하는 솔레노이드의 OR 의미가 순회 순서에 따라 깨진다

- 근거: 일반 장치 코일은 `coilByLabel`로 OR 병합한다(`src/core/sim/engine.ts:220-245`). 솔레노이드는 각 부품을 순회하며 ON이면 add, OFF이면 delete한다(`src/core/sim/engine.ts:233-235`).
- 영향: 같은 라벨의 솔레노이드 중 하나가 ON이고 뒤에서 처리되는 다른 하나가 OFF이면 활성 라벨이 삭제된다.
- 권고: 솔레노이드도 라벨별 OR 집계를 끝낸 후 한 번에 집합을 갱신한다. 구성 순서를 바꾼 회귀 테스트를 추가한다.

#### M3. PRD가 요구한 포트 타입 불일치 실행 전 경고가 없다

- 요구: `docs/PRD.md:69`.
- 실제: 편집 UI에서는 연결을 거부하지만(`src/core/model/operations.ts:146-175`), 파일 파서는 잘못된 wire를 수락하고 `validateForSimulation()`은 endpoint/kind 일치를 검사하지 않는다(`src/core/sim/validate.ts:8-61`).
- 영향: 잘못된 외부 문서가 실행 전 진단 없이 솔버로 전달된다.
- 권고: H1의 문서 경계 검증을 우선 적용하고, 사용자에게 회로 문제를 한 번에 보여줄 필요가 있다면 실행 전 검증에도 참조·kind 진단을 추가한다.

#### M4. 부품 삭제가 PLC `ioMap`의 해당 참조를 정리하지 않는다

- 근거: `src/core/model/operations.ts:59-73`은 부품, 배선, equipmentLayout만 정리한다. `ioMap`은 남는다.
- 영향: 삭제된 부품 매핑이 문서에 잔존하고 PLC 패널에는 “삭제됨” 항목이 남으며, 실행 시 입력은 false 또는 출력은 무시되는 조용한 오동작이 생긴다.
- 권고: 부품 삭제 시 해당 `ioMap` 항목도 제거하고 저장→불러오기 및 undo/redo 회귀 테스트를 추가한다.

#### M5. 구현된 전기/PLC 틱 의미가 아키텍처 문서와 다르다

- 문서: `docs/ARCHITECTURE.md:94-105`는 PLC 출력 뒤 전기 재솔브, 릴레이 상태 변경 시 한 번만 재평가하고 긴 체인은 다음 틱에 진행한다고 정의한다.
- 구현: `src/core/sim/engine.ts:204-250`은 PLC 전에 최대 5회 고정점 반복을 수행하고, `src/core/sim/engine.ts:298-325`의 PLC 출력 뒤에는 전기 재솔브가 없다.
- 영향: 교육용으로 관찰되는 릴레이/PLC 전이 타이밍이 문서와 달라질 수 있으며 H2 같은 통합 결함을 숨긴다.
- 권고: 한 가지 틱 계약을 선택해 코드와 문서를 일치시키고, 틱 단위 상태 전이 테스트로 고정한다.

#### M6. 문서가 요구하는 UI 회귀 테스트가 없고 60fps 주장이 엔진 벤치마크만으로 검증된다

- 문서: `docs/ARCHITECTURE.md:142-146`은 배선·스냅·저장/불러오기 UI 컴포넌트 테스트를 요구한다. `docs/PRD.md:74`는 100개 부품에서 60fps를 요구한다.
- 실제: 테스트 10개 파일은 모두 `src/core/**`에 있다. `src/core/sim/__tests__/performance.test.ts:11-43`은 Node에서 `engine.tick()` 평균만 측정하고 React/SVG 렌더링 프레임은 측정하지 않는다.
- 영향: 포인터 조작, 파일 UI, undo/redo, 장비 배치, 50Hz 전체 SVG 갱신의 회귀가 현재 테스트를 통과할 수 있다.
- 권고: 핵심 사용자 흐름의 브라우저 테스트와 100개 회로의 프레임/long-task 수용 테스트를 추가한다.

#### M7. localStorage 쓰기 실패가 성공 메시지 대신 처리되지 않은 예외가 된다

- 근거: `src/core/storage/index.ts:47-49`의 `setItem()` 예외가 그대로 전파되고, `src/ui/editor/Toolbar.tsx:53-59`은 저장 성공만 가정한다.
- 영향: 용량 초과, 사생활 보호 모드, 저장소 권한 제한에서 버튼 동작이 실패하고 사용자에게 복구 가능한 오류 안내가 나오지 않는다.
- 권고: 저장 경계에서 typed result를 반환하고 UI에서 용량/권한 실패를 한국어 상태 메시지로 표시한다.

#### M8. 문법상 유효하지만 구조가 잘못된 localStorage 값이 앱 렌더링을 중단시킨다

- 근거: `src/core/storage/index.ts:39-58`은 JSON 파싱 성공 후 저장소 shape와 entry를 검증하지 않고 `Object.entries`, `entry.savedAt`, `localeCompare`를 사용한다. Toolbar는 렌더 중 바로 `browserStorage.list()`를 호출한다(`src/ui/editor/Toolbar.tsx:48-51`).
- 영향: 같은 오리진의 스크립트, 이전 버전, 개발자 도구 등으로 `hyd.circuits.v1`에 `null`이나 잘못된 entry가 저장되면 Toolbar 렌더가 예외로 중단될 수 있다.
- 권고: 저장소 값도 외부 경계로 보고 전체 shape를 파싱한다. 손상된 항목은 격리하고 사용자에게 복구/초기화 안내를 제공한다.

#### M9. 드래그 중 undo가 redo 이력을 지울 수 있다

- 근거: 드래그 시작 문서는 별도 `dragStartDoc`에 보관되고 pointer-up에서 히스토리로 커밋된다(`src/ui/editor/store.ts:157-175`). undo는 문서와 future만 바꾸고 `dragStartDoc`은 정리하지 않는다(`src/ui/editor/store.ts:259-268`). 전역 undo 단축키도 드래그 상태와 무관하게 실행된다(`src/app/App.tsx:20-35`).
- 영향: 드래그 도중 undo한 뒤 pointer-up하면 남아 있던 드래그 시작 스냅숏이 다시 커밋되면서 redo 이력이 사라질 수 있다.
- 권고: 드래그 중 undo/redo를 차단하거나, undo/redo 전에 드래그를 원자적으로 취소/종료하고 `dragStartDoc`을 항상 정리한다. 포인터 이벤트 기반 회귀 테스트를 추가한다.

#### M10. 같은 방향의 일직선 포트 라우팅이 목적지를 지나 역주행할 수 있다

- 근거: `src/core/routing.ts:35-55`의 직선/스텁 규칙에서 `(0,0) right → (100,0) right` 같은 같은 방향 포트가 목적지를 지난 뒤 되돌아오는 겹친 경로를 만든다.
- 영향: 배선이 시각적으로 중첩되고 연결 방향을 오해하게 만들어 교육용 회로도의 가독성을 해친다.
- 권고: 직선 최적화에 서로 마주 보는 방향 조건을 포함하고, 같은 방향 포트에는 명시적인 dogleg/U 경로를 사용한다. 네 방향 조합을 테이블 테스트로 고정한다.

#### M11. PLC 매핑된 혼합 도메인 부품은 유체 포트 미연결 경고까지 면제된다

- 근거: `src/core/sim/validate.ts:22-29,40-41`은 `ioMap`에 포함된 componentId를 부품 단위로 전부 건너뛴다. 압력 스위치처럼 유체 포트와 전기 접점 의미를 함께 가진 부품도 유체 포트 검사를 받지 않는다.
- 영향: PLC 입력으로 매핑한 압력 스위치의 압력 포트가 연결되지 않아도 실행 전 경고가 나오지 않는다.
- 권고: PLC 매핑 때문에 배선이 불필요한 전기 측 의미만 예외 처리하고, 유체 포트 검사는 계속 수행한다.

### LOW (3)

#### L1. README의 개수 정보가 현재 코드와 다르다

- `README.md:34,39`는 공압 19종, 예제 17종, 테스트 39개라고 적지만 현재 등록 공압 정의는 20종, 예제는 18종, Vitest 테스트는 50개다.
- 권고: 수치를 갱신하거나 테스트에서 목록 수를 산출해 문서 드리프트를 줄인다.

#### L2. 파일 선택 취소 시 Promise가 완료되지 않는다

- 근거: `src/app/file.ts:56-73`은 change 이벤트만 resolve하며 취소 경로를 완료하지 않는다. 주석의 “resolve하지 않아도 GC됨”은 호출자 관점의 완료 계약을 충족하지 않는다.
- 영향: 현재 UI는 계속 사용할 수 있지만 `handleOpen()` 비동기 호출은 영구 대기 상태로 남는다.
- 권고: `cancel` 이벤트 또는 focus 복귀 후 파일 부재를 처리해 명시적으로 취소 결과를 반환한다.

#### L3. 언어 전환 시 문서 언어 메타데이터가 갱신되지 않는다

- 근거: `index.html:2`는 항상 `lang="ko"`이고 `src/ui/i18n/index.ts:107-119`은 상태와 localStorage만 변경한다.
- 영향: 영어 UI로 전환해도 보조기술과 브라우저 번역 기능은 한국어 문서로 인식한다.
- 권고: 언어 상태가 바뀔 때 `document.documentElement.lang`도 `ko`/`en`으로 동기화한다.

## 아키텍처 관찰 사항

### BLOCK

- 외부 문서 경계가 풍부한 그래프 모델을 검증하지 않은 채 강제 단언한다. 모든 하류 모듈이 유효한 참조와 타입을 가정하므로 H1을 해결하기 전에는 아키텍처 상태를 CLEAR로 볼 수 없다.
- PRD 필수이자 ROADMAP 완료 대상으로 노출된 `hyd.relief`가 편집 가능한 설정과 기호만 있고 실행상 no-op이다. 범용 pressure-relief 역할, 압력 상한 단계, 등록 적합성 검증을 갖추기 전에는 기능 완결성 측면에서도 CLEAR가 될 수 없다.

### IP 출시 게이트

- 두 상용 제품을 참고 모델로 명시한 이력은 있으나 최초 커밋 이전 제작 기록과 자산별 provenance가 없다. 검토 범위에서 직접 복제 정황은 미발견이지만, 공개·재공개 대상의 독립 개발 경위와 권리 귀속을 검증할 내부 증빙이 부족하다.
- ISO 1219, KS·교재 기반 기호, 자격시험 유형 예제의 정확한 출처·판본·독립 작성 경위·이용 범위가 기록되지 않았다. 이는 아키텍처 결함과 구분되는 보수적 내부 배포 게이트다.
- 런타임 패키지 NOTICE/SBOM과 코드·문서·자산의 chain of title이 미정리다. 프로젝트 자체 LICENSE 부재는 소유자의 배포를 자동 차단하지 않지만 제3자 재사용 조건과 고지 책임을 불명확하게 한다.

### WATCH

- `ComponentInstance.properties`가 `Record<string, unknown>`이고 엔진은 `label`, `mode`, `preset`, `strokeTime`, `speed`, `openness`, `pressure` 같은 문자열 키를 직접 읽는다. 새 부품 정의의 속성 키가 어긋나도 컴파일은 성공하고 기본값으로 조용히 오동작할 수 있다. 등록 시 behavior별 필수 propertySchema를 검증하는 적합성 테스트가 필요하다.
- `SimulationEngine`이 가변 `CircuitDocument` 참조를 그대로 보관한다. 현재 UI는 실행 중 편집을 잠가 위험을 줄이지만, 코어 API 계약은 readonly 또는 생성 시 정규화/복제로 강화하는 편이 안전하다.
- 유체 성능 테스트는 25개의 작은 독립 회로를 사용한다. 긴 단일 연결망이나 순환망의 최악 조건을 대표하지 않으므로 연결 체인·순환 토폴로지 벤치마크가 필요하다.
- `public/sw.js:15-16`은 같은 오리진의 모든 다른 캐시를 삭제한다. 다른 앱과 오리진을 공유할 수 있다면 `hyd-` 접두사의 구버전 캐시만 정리해야 한다.
- `README.md:17`, `docs/PRD.md:24,53-59`, PLC 소스와 프로덕션 번들에 XG5000 명칭이 남지만 비제휴·비호환 고지가 없다. 사실적 교육 설명에 필요하면 사용 범위를 최소화하고 LS ELECTRIC과 무관한 독립 구현임을 인접 위치에서 밝혀야 한다.

## 잘된 점

- `src/core/`가 React와 독립적이고 Node/Vitest에서 단독 실행된다.
- 구체 부품 타입이 아니라 `Behavior` 판별 유니온과 `role`을 중심으로 솔버가 동작해 “새 부품마다 솔버 분기 금지” 원칙을 대체로 지킨다.
- 모델 변경 함수가 불변 문서를 반환하고 undo/redo 스냅숏 구조와 잘 맞는다.
- 공압·유압·전기·PLC의 대표 시뮬레이션과 A+B+A−B− 골든 시나리오가 자동화되어 있다.
- schemaVersion 2와 v1→v2 equipmentLayout 마이그레이션이 있으며 관련 회귀 테스트가 있다.
- 빌드가 엄격한 TypeScript 검사와 함께 성공하고 프로덕션 번들 크기도 과도하지 않다.

## 권장 수정 순서

아래 순서는 사용자 제보 기능 복구를 먼저 배치한 것이다. H6와 H1은 기능·아키텍처 **BLOCK** 해제에 필수이며, 6~9는 공개 경로 활성화·재공개와 향후 상용 배포의 별도 IP 게이트다.

1. H6 릴리프 밸브에 범용 압력 제한 동작과 경계·브라우저 테스트를 추가한다.
2. H1 문서 경계 검증을 도입하고 잘못된 문서가 앱을 중단시키지 않도록 한다.
3. H2 PLC 출력→전기 장치 상태→재솔브 의미를 정의하고 통합 테스트로 고정한다.
4. 셔틀 역급기, 2압밸브 압력 레벨, 탠덤 센터 탱크 상태, 감압밸브 역방향 cap을 역할별 테스트와 함께 수정한다.
5. H3/H4의 누락 범위를 구현하거나 제품 문서의 완료 범위를 현실에 맞게 정정한다.
6. 자산·예제·문구·부품 데이터의 provenance 원장을 만들고 기여자별 경쟁 제품 자료 접근·실제 이용 범위를 사실대로 재구성한다.
7. ISO 1219·KS·교재·자격시험 자료의 판본과 이용 범위, V-AMT EULA를 확보해 관할별 전문 법률 검토를 받는다.
8. 두 참고 제품과의 UI side-by-side 독립 검토를 수행하고, 앞으로의 독립 구현 절차와 동시대 기록을 운영한다.
9. 프로젝트 LICENSE, 런타임 SBOM/NOTICE, XG5000 비제휴·`.xgp` 비호환 고지, 코드·문서·자산 chain of title을 정리한다.
10. H5 미저장 변경 보호와 M1 오프라인 앱 셸 캐시를 수정한다.
11. 핵심 UI/E2E, 장비 스프라이트 정합성, 실제 렌더링 성능 테스트를 추가한다.
12. 나머지 참조 정리, 저장 실패 처리, 문서 드리프트를 정리한다.

## 최종 판정

- 코드 리뷰어 권고: **REQUEST CHANGES**
- 아키텍처 상태: **BLOCK**
- 종합 권고: **REQUEST CHANGES**

현재 상태는 정상 내장 예제와 코어 테스트 경로에서는 강하지만, 릴리프 밸브가 기능하지 않고 파일 저장/교환 경계와 PLC 통합에도 재현 가능한 결함이 있다. 요소별 감사에서 셔틀 역급기, 2압밸브 압력 레벨, 탠덤 센터 상태, 장치 label 오염도 추가 확인됐다. 위 우선순위 1~5를 해결한 뒤 재검토하는 것이 적절하다.

저작권/IP 측면에서는 검토 범위에서 두 참고 제품의 코드·그래픽·로고·파일을 직접 복제했다는 정황을 찾지 못했지만 침해 여부를 확정할 수는 없다. 상용 제품 참고 사실, 완성본 단일 최초 커밋, 자산·교육자료 provenance, 표준 이용범위, V-AMT EULA, 런타임 고지, chain of title이 미확정이므로 공개 경로 활성화·재공개와 향후 상용 배포에는 별도 내부 **BLOCK**이 필요하다. README와 배포 워크플로상 공개 배포 경로가 이미 운영 범위에 있으므로 Pages가 활성 상태라면 우선순위 6~9는 즉시 정리 과제이고, 비활성 상태라면 재공개 전 필수 게이트다.
